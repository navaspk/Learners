package com.sample.clean.ui.home.adapter

/**
 * To get different type of items, so that we can differentiate easily with different
 * layout, in future we can add multiple types items in home screen
 */
enum class CommunityItemType {
    TYPE_COMMUNITY_HOME_ITEM
}
